openapi: 3.0.3
info:
  title: Kinvo API
  version: 1.0.0
  description: >-
    REST API for Kinvo, a multi-mode social connection app.


    ## Every response has the same shape


    ```jsonc

    { "success": true,  "data": {}, "meta": null }

    { "success": false, "error": { "code": "AUTH_TOKEN_EXPIRED", "message":
    "...", "details": null } }

    ```


    Branch on `error.code`, never on `message`. Codes are a stable contract;
    messages are user-facing text.


    ## Authentication


    Sign in, then send `Authorization: Bearer <access_token>` on every request.


    | Code | Meaning | What the app should do |

    | --- | --- | --- |

    | `AUTH_REQUIRED` | No token sent | Go to sign-in |

    | `AUTH_TOKEN_EXPIRED` | Access token aged out | Refresh silently, retry the
    request |

    | `AUTH_TOKEN_INVALID` | Bad, revoked, or reused | Log the user out |


    Refresh tokens **rotate**: every call to `/auth/refresh` returns a new one
    and invalidates the old. Store the newest. Reusing an old one is treated as
    theft and signs that device out.


    ## Test accounts


    `sarah.dev@kinvo.test` / `kinvo-dev-password` — and 29 more
    `*.dev@kinvo.test` users, same password.


    ## Not yet available on staging


    Google, Apple and phone OTP return `SERVICE_UNAVAILABLE` — those provider
    accounts are not configured yet. Build the screens; the contract will not
    change.


    ## Units and formats


    - Timestamps: UTC ISO-8601 with `Z`, field names end in `_at`

    - Dates: `YYYY-MM-DD`

    - **Distances: metres.** Format to miles in the app

    - Money: integer minor units plus a currency code

    - A missing value is `null`, never an omitted key. Empty lists are `[]`
servers:
  - url: https://dm9o5kgscmnxv.cloudfront.net
    description: Staging
tags:
  - name: Meta
    description: Health and configuration
  - name: Auth
    description: Registration, sign-in, tokens
  - name: Onboarding
    description: Getting an account from pending to active
  - name: Profile
    description: Profile content and public views
  - name: Media
    description: Uploads, photos
  - name: Verification
    description: Identity verification
  - name: Discovery
    description: Decks, swipes, matches forming, boosts
  - name: Matches
    description: The Matches and Archived tabs, unmatch, extend
  - name: Chat
    description: Conversations, messages, read state
  - name: Moderation
    description: Pre-send checks and the moderation queue
  - name: Notifications
    description: Feed, push tokens, preferences, badge counts
  - name: Safety
    description: Reports, blocks, trusted contacts, live location, emergency
  - name: Plans
    description: Date planning and trusted-contact sharing
  - name: Venues
    description: Curated places, search, save, suggest
  - name: Subscriptions
    description: Product catalogue and your current subscription
  - name: Calls
    description: Video call lifecycle, room tokens, in-call safety
  - name: Entitlements
    description: Plan features and daily quotas
  - name: Modes
    description: The eight connection modes and their per-mode preferences
  - name: Settings
    description: Appearance, privacy, snooze, connected devices
paths:
  /api/v1/health:
    get:
      tags:
        - Meta
      summary: Liveness probe
      description: Checks no dependency on purpose. A liveness probe that fails when
        the database blips makes the orchestrator kill healthy processes.
      security: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/health/ready:
    get:
      tags:
        - Meta
      summary: Readiness probe
      description: Reports whether Postgres and Redis are reachable.
      security: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
        "503":
          description: SERVICE_UNAVAILABLE — This service is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: SERVICE_UNAVAILABLE
                  message: This service is temporarily unavailable.
                  details: null
  /api/v1/config:
    get:
      tags:
        - Meta
      summary: Enum catalogues and feature flags
      description: Mode list with the label each mode renders for its deck action,
        interest tags, prompt questions, and report reasons. Fetch on launch and
        cache. Adding a mode or an interest ships through here, never through an
        app release.
      security: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/register:
    post:
      tags:
        - Auth
      summary: Create an account with email and password
      description: Rejects under-18 from the date of birth. Returns 409 if the address
        already exists — including when it was created by Google or Apple
        sign-in, in which case use forgot-password to set a password rather than
        registering.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  minLength: 1
                  maxLength: 320
                  format: email
                password:
                  type: string
                  minLength: 8
                  maxLength: 72
                display_name:
                  type: string
                  minLength: 1
                  maxLength: 50
                date_of_birth:
                  type: string
                  pattern: ^\d{4}-\d{2}-\d{2}$
                device_id:
                  type: string
                  maxLength: 128
              required:
                - email
                - password
                - display_name
                - date_of_birth
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/login:
    post:
      tags:
        - Auth
      summary: Sign in
      description: An unknown email and a wrong password return an identical response,
        deliberately — anything else lets an attacker enumerate registered
        addresses.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  minLength: 1
                  maxLength: 320
                  format: email
                password:
                  type: string
                  minLength: 1
                device_id:
                  type: string
                  maxLength: 128
              required:
                - email
                - password
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_INVALID_CREDENTIALS — That email or password is incorrect.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_INVALID_CREDENTIALS
                  message: That email or password is incorrect.
                  details: null
        "403":
          description: ACCOUNT_SUSPENDED — Your account has been suspended.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: ACCOUNT_SUSPENDED
                  message: Your account has been suspended.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/refresh:
    post:
      tags:
        - Auth
      summary: Exchange a refresh token for a new pair
      description: Refresh tokens ROTATE. Store the new one every time. Presenting a
        token that has already been rotated is treated as theft and revokes
        every token in that family, signing that device out.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                refresh_token:
                  type: string
                  minLength: 1
              required:
                - refresh_token
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/logout:
    post:
      tags:
        - Auth
      summary: Sign out this device
      description: Revokes the presented token family only, so other devices stay
        signed in. Always succeeds, even for an unknown token.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                refresh_token:
                  type: string
                  minLength: 1
              required:
                - refresh_token
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/forgot-password:
    post:
      tags:
        - Auth
      summary: Request a password reset
      description: The response is identical whether or not the address is registered.
        Outside production the reset token is returned in the body so the flow
        is testable before email delivery exists.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  minLength: 1
                  maxLength: 320
                  format: email
              required:
                - email
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/reset-password:
    post:
      tags:
        - Auth
      summary: Redeem a reset token
      description: Single use, one-hour expiry. Revokes every existing session on success.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                token:
                  type: string
                  minLength: 1
                password:
                  type: string
                  minLength: 8
                  maxLength: 72
              required:
                - token
                - password
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/change-password:
    post:
      tags:
        - Auth
      summary: Change password while signed in
      description: Requires the current password. Revokes every session on success.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                current_password:
                  type: string
                  minLength: 1
                new_password:
                  type: string
                  minLength: 8
                  maxLength: 72
              required:
                - current_password
                - new_password
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: BAD_REQUEST — We could not process that request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: BAD_REQUEST
                  message: We could not process that request.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/auth/otp/send:
    post:
      tags:
        - Auth
      summary: Send a one-time code by SMS
      description: NOT YET AVAILABLE on staging — returns SERVICE_UNAVAILABLE until
        Twilio is configured. The response never reveals whether a number is
        registered.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                phone:
                  type: string
                  pattern: ^\+[1-9]\d{7,14}$
              required:
                - phone
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
        "503":
          description: SERVICE_UNAVAILABLE — This service is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: SERVICE_UNAVAILABLE
                  message: This service is temporarily unavailable.
                  details: null
  /api/v1/auth/otp/verify:
    post:
      tags:
        - Auth
      summary: Verify a one-time code and sign in
      description: NOT YET AVAILABLE on staging. A number with no account creates a
        pending one, which cannot use discovery or chat until onboarding
        supplies a date of birth.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                phone:
                  type: string
                  pattern: ^\+[1-9]\d{7,14}$
                code:
                  type: string
                  pattern: ^\d{4,10}$
                display_name:
                  type: string
                  minLength: 1
                  maxLength: 50
                device_id:
                  type: string
                  maxLength: 128
              required:
                - phone
                - code
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_INVALID_CREDENTIALS — That email or password is incorrect.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_INVALID_CREDENTIALS
                  message: That email or password is incorrect.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
        "503":
          description: SERVICE_UNAVAILABLE — This service is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: SERVICE_UNAVAILABLE
                  message: This service is temporarily unavailable.
                  details: null
  /api/v1/auth/google:
    post:
      tags:
        - Auth
      summary: Sign in with Google
      description: NOT YET AVAILABLE on staging. Send the ID token from the Google
        SDK. A verified email matching an existing account links to it rather
        than creating a duplicate.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id_token:
                  type: string
                  minLength: 1
                display_name:
                  type: string
                  minLength: 1
                  maxLength: 50
                device_id:
                  type: string
                  maxLength: 128
              required:
                - id_token
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
        "503":
          description: SERVICE_UNAVAILABLE — This service is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: SERVICE_UNAVAILABLE
                  message: This service is temporarily unavailable.
                  details: null
  /api/v1/auth/apple:
    post:
      tags:
        - Auth
      summary: Sign in with Apple
      description: NOT YET AVAILABLE on staging. Apple sends the display name only on
        the FIRST authorisation and never again — capture it then and pass it as
        display_name, or the account has no name.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id_token:
                  type: string
                  minLength: 1
                display_name:
                  type: string
                  minLength: 1
                  maxLength: 50
                device_id:
                  type: string
                  maxLength: 128
              required:
                - id_token
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
        "503":
          description: SERVICE_UNAVAILABLE — This service is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: SERVICE_UNAVAILABLE
                  message: This service is temporarily unavailable.
                  details: null
  /api/v1/auth/me:
    get:
      tags:
        - Auth
      summary: The signed-in account
      description: Account state and linked identities. Profile content is on /users/me.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: ACCOUNT_SUSPENDED — Your account has been suspended.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: ACCOUNT_SUSPENDED
                  message: Your account has been suspended.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/onboarding:
    get:
      tags:
        - Onboarding
      summary: Progress and what is still missing
      description: Returns each step with is_complete, plus a `missing` array. Drive
        the onboarding screens from this rather than tracking state client-side.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/onboarding/complete:
    post:
      tags:
        - Onboarding
      summary: Finish onboarding
      description: Moves the account from pending to active. Fails with
        ONBOARDING_INCOMPLETE and a `missing` list until every requirement is
        met. Idempotent.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: ACCOUNT_SUSPENDED — Your account has been suspended.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: ACCOUNT_SUSPENDED
                  message: Your account has been suspended.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/onboarding/date-of-birth:
    post:
      tags:
        - Onboarding
      summary: Set date of birth
      description: For accounts created by Google, Apple or phone, which arrive
        without one. Applies the under-18 check. Refuses to change a date that
        is already set.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                date_of_birth:
                  type: string
                  pattern: ^\d{4}-\d{2}-\d{2}$
              required:
                - date_of_birth
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/users/me:
    get:
      tags:
        - Profile
      summary: Own full profile
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    patch:
      tags:
        - Profile
      summary: Update profile fields
      description: Omit a field to leave it unchanged; send null to clear it. Those
        are different, so do not send nulls for untouched fields.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                  minLength: 1
                  maxLength: 50
                bio:
                  type: string
                  maxLength: 500
                  nullable: true
                job_title:
                  type: string
                  maxLength: 100
                  nullable: true
                organisation:
                  type: string
                  maxLength: 100
                  nullable: true
                education:
                  type: string
                  enum:
                    - high_school
                    - undergraduate
                    - postgraduate
                    - doctorate
                    - other
                    - prefer_not_to_say
                  nullable: true
                height_cm:
                  type: integer
                  minimum: 120
                  maximum: 250
                  nullable: true
                city:
                  type: string
                  maxLength: 120
                  nullable: true
                country:
                  type: string
                  minLength: 2
                  maxLength: 2
                  nullable: true
                drinking:
                  type: string
                  enum:
                    - never
                    - rarely
                    - socially
                    - regularly
                    - prefer_not_to_say
                  nullable: true
                smoking:
                  type: string
                  enum:
                    - never
                    - rarely
                    - socially
                    - regularly
                    - prefer_not_to_say
                  nullable: true
                exercise:
                  type: string
                  enum:
                    - never
                    - sometimes
                    - often
                    - daily
                    - prefer_not_to_say
                  nullable: true
                diet:
                  type: string
                  enum:
                    - omnivore
                    - vegetarian
                    - vegan
                    - pescatarian
                    - halal
                    - kosher
                    - other
                    - prefer_not_to_say
                  nullable: true
                pets:
                  type: string
                  enum:
                    - none
                    - dog
                    - cat
                    - other
                    - multiple
                    - prefer_not_to_say
                  nullable: true
                children:
                  type: string
                  enum:
                    - none
                    - have_children
                    - want_children
                    - do_not_want_children
                    - open
                    - prefer_not_to_say
                  nullable: true
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    delete:
      tags:
        - Profile
      summary: Delete the account
      description: Soft delete. Revokes every session and removes the user from every
        listing.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/users/me/location:
    patch:
      tags:
        - Profile
      summary: Set location
      description: Longitude and latitude, in that order. Stored as a PostGIS point.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                longitude:
                  type: number
                  minimum: -180
                  maximum: 180
                latitude:
                  type: number
                  minimum: -90
                  maximum: 90
                city:
                  type: string
                  maxLength: 120
                country:
                  type: string
                  minLength: 2
                  maxLength: 2
              required:
                - longitude
                - latitude
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/users/me/interests:
    put:
      tags:
        - Profile
      summary: Replace the interest set
      description: Send the complete list. Slugs come from /config.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                interests:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  maxItems: 10
              required:
                - interests
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/users/me/prompts:
    put:
      tags:
        - Profile
      summary: Replace prompt answers
      description: Send the complete list. Slugs come from /config.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                prompts:
                  type: array
                  items:
                    type: object
                    properties:
                      slug:
                        type: string
                        minLength: 1
                      answer:
                        type: string
                        minLength: 1
                        maxLength: 300
                    required:
                      - slug
                      - answer
                    additionalProperties: false
                  maxItems: 3
              required:
                - prompts
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/users/me/preview:
    get:
      tags:
        - Profile
      summary: How others see you
      description: The caller rendered through the exact public projection, so the
        preview cannot drift from what a stranger actually receives.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/users/{id}:
    get:
      tags:
        - Profile
      summary: Another user's public profile
      description: Returns 404 — never 403 — when the viewer is blocked, the account
        is suspended or deleted, or the id never existed. All four are
        deliberately indistinguishable.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/media/uploads:
    post:
      tags:
        - Media
      summary: Request an upload URL
      description: Step 1 of 2. Returns a presigned URL plus the headers you MUST
        send. PUT the bytes straight to that URL — they never pass through this
        API — then call the complete endpoint.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                purpose:
                  type: string
                  enum:
                    - profile_photo
                    - chat_image
                    - chat_video
                    - voice_note
                    - verification_document
                    - report_evidence
                mime_type:
                  type: string
                  minLength: 1
                  maxLength: 128
                size_bytes:
                  type: integer
                  exclusiveMinimum: true
                  minimum: 0
                duration_ms:
                  type: integer
                  exclusiveMinimum: true
                  minimum: 0
              required:
                - purpose
                - mime_type
                - size_bytes
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "413":
          description: FILE_TOO_LARGE — That file is too large.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FILE_TOO_LARGE
                  message: That file is too large.
                  details: null
        "415":
          description: UNSUPPORTED_MEDIA_TYPE — That file type is not supported.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: UNSUPPORTED_MEDIA_TYPE
                  message: That file type is not supported.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
        "503":
          description: SERVICE_UNAVAILABLE — This service is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: SERVICE_UNAVAILABLE
                  message: This service is temporarily unavailable.
                  details: null
  /api/v1/media/uploads/{id}/complete:
    post:
      tags:
        - Media
      summary: Confirm an upload
      description: Step 2 of 2. The server asks storage what actually arrived and
        records that, not what was declared. An upload that is never completed
        can never be attached to anything.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: BAD_REQUEST — We could not process that request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: BAD_REQUEST
                  message: We could not process that request.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/media/uploads/{id}/url:
    get:
      tags:
        - Media
      summary: Get a viewable URL for your own upload
      description: Time-limited. Both buckets are private, so URLs are minted per request.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/media/uploads/{id}:
    delete:
      tags:
        - Media
      summary: Delete an upload
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/media/photos:
    get:
      tags:
        - Media
      summary: List profile photos
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    post:
      tags:
        - Media
      summary: Attach a completed upload as a photo
      description: Maximum six. The first becomes primary automatically.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                upload_id:
                  type: string
                  format: uuid
                width:
                  type: integer
                  exclusiveMinimum: true
                  minimum: 0
                height:
                  type: integer
                  exclusiveMinimum: true
                  minimum: 0
              required:
                - upload_id
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: BAD_REQUEST — We could not process that request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: BAD_REQUEST
                  message: We could not process that request.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/media/photos/reorder:
    patch:
      tags:
        - Media
      summary: Reorder photos
      description: Send every photo id exactly once, in the order you want. First
        becomes primary.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                photo_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
                  maxItems: 6
              required:
                - photo_ids
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/media/photos/{id}/primary:
    patch:
      tags:
        - Media
      summary: Set the primary photo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/media/photos/{id}:
    delete:
      tags:
        - Media
      summary: Delete a photo
      description: Remaining photos close the gap in the ordering.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/verification:
    get:
      tags:
        - Verification
      summary: Verification status
      description: Wizard position and whether the badge is earned.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    post:
      tags:
        - Verification
      summary: Start verification
      description: Step 1 of 3. One attempt at a time.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                method:
                  type: string
                  enum:
                    - photo
                    - government_id
                    - social
              required:
                - method
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/verification/{id}/document:
    post:
      tags:
        - Verification
      summary: Attach the document
      description: Step 2 of 3. The upload must have purpose verification_document —
        those go to a separate, stricter bucket and a profile photo cannot be
        submitted as an ID.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                upload_id:
                  type: string
                  format: uuid
              required:
                - upload_id
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: BAD_REQUEST — We could not process that request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: BAD_REQUEST
                  message: We could not process that request.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/verification/{id}/submit:
    post:
      tags:
        - Verification
      summary: Submit for review
      description: Step 3 of 3.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: BAD_REQUEST — We could not process that request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: BAD_REQUEST
                  message: We could not process that request.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/discovery/{mode}/deck:
    get:
      tags:
        - Discovery
      summary: Today’s deck for one mode
      description: Cursor-paginated cards, precomputed once per user per mode per UTC
        day so the order is stable while scrolling. `distance_metres` is metres
        — format to miles on the client. Cards already swiped never reappear.
        Returns 400 if the mode is switched off or the account has no location.
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/discovery/{mode}/swipe:
    post:
      tags:
        - Discovery
      summary: Pass, like, or super like
      description: The three actions are identical in every mode — only the label the
        app renders changes, and that comes from GET /config. Responds with
        `is_match` and the match when the target had already liked you IN THIS
        MODE. Likes and super likes spend daily quota; a pass is free. A second
        swipe on the same person in the same mode is 409.
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                target_id:
                  type: string
                  format: uuid
                action:
                  type: string
                  enum:
                    - pass
                    - like
                    - super_like
              required:
                - target_id
                - action
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "422":
          description: QUOTA_EXCEEDED — You have reached your limit for today.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: QUOTA_EXCEEDED
                  message: You have reached your limit for today.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/discovery/{mode}/rewind:
    post:
      tags:
        - Discovery
      summary: Undo the last swipe in this mode
      description: Restores the profile to the deck and refunds the quota it spent. If
        that swipe had created a match, the match is removed too. Premium.
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: PREMIUM_REQUIRED — This feature is available with Premium.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: PREMIUM_REQUIRED
                  message: This feature is available with Premium.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/discovery/{mode}/likes-you:
    get:
      tags:
        - Discovery
      summary: Who liked you, in this mode
      description: "The Requests tab: profiles, not messages. People you have already
        answered — matched or passed — are excluded. Premium."
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: PREMIUM_REQUIRED — This feature is available with Premium.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: PREMIUM_REQUIRED
                  message: This feature is available with Premium.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/discovery/{mode}/boost:
    post:
      tags:
        - Discovery
      summary: Raise your ranking for a window
      description: Ranking only. A boost moves you up decks you already qualified for
        and can never place you into one a filter excluded you from. 409 while a
        boost is already running. Premium.
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: PREMIUM_REQUIRED — This feature is available with Premium.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: PREMIUM_REQUIRED
                  message: This feature is available with Premium.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/discovery/{mode}/stats:
    get:
      tags:
        - Discovery
      summary: Counters for the empty state
      description: "Everything the end-of-deck screen needs in one call: swipe counts,
        active matches, how many people are waiting in likes-you, cards left
        today, any running boost, and what is left of the daily allowance.
        `likes_received` is a count only — the profiles stay behind the
        paywall."
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/matches:
    get:
      tags:
        - Matches
      summary: The Matches tab
      description: "Cursor-paginated, newest first, with the conversation id, unread
        count, and last message preview on each row so the list renders in one
        request. `?archived=true` is the Archived tab. **The Requests tab is a
        different endpoint** — `GET /discovery/{mode}/likes-you` — because
        Requests holds profiles who liked you, not conversations. `is_writable`
        is false when the pair is blocked, the match expired, or it was
        unmatched: hide the composer on it rather than discovering the state
        from a rejected send."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/matches/{id}:
    get:
      tags:
        - Matches
      summary: One match
      description: Answers 404 for a match that is not yours, was unmatched, or never
        existed — all three identically.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    delete:
      tags:
        - Matches
      summary: Unmatch
      description: "Final and symmetric: the match disappears for both people at once.
        The swipes stay, so neither is offered the other again in this mode. Who
        unmatched is recorded for later report investigations and is never
        exposed to the other person."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/matches/{id}/extend:
    post:
      tags:
        - Matches
      summary: Push back the expiry
      description: Extends from the current expiry rather than from now, so extending
        a match with days left adds the window instead of shortening it. An
        already-expired match becomes live again. Premium.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: PREMIUM_REQUIRED — This feature is available with Premium.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: PREMIUM_REQUIRED
                  message: This feature is available with Premium.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/conversations:
    get:
      tags:
        - Chat
      summary: Conversation list
      description: "Ordered by activity, not creation. Carries the other participant,
        unread count, and last message preview so the list needs no follow-up
        calls. There is no endpoint that creates a conversation: one is created
        with its match, and users cannot message before matching."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/conversations/unread-count:
    get:
      tags:
        - Chat
      summary: Total unread, for the app badge
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/conversations/{id}:
    get:
      tags:
        - Chat
      summary: One conversation
      description: The chat header. `mode` is inherited from the match and never
        changes. `is_writable` false means the composer should be hidden.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    patch:
      tags:
        - Chat
      summary: Archive or mute
      description: Per user, not shared — archiving does not archive it for the other
        person. A new message pulls the thread back out of the archive.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                is_archived:
                  type: boolean
                is_muted:
                  type: boolean
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/conversations/{id}/messages:
    get:
      tags:
        - Chat
      summary: Message history, newest first
      description: "**This list paginates BACKWARDS**, unlike every other list in this
        API: the first page is the most recent messages and the cursor walks
        into the past, because a chat opens at the bottom. A blocked pair keeps
        its history readable; only sending is refused."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    post:
      tags:
        - Chat
      summary: Send a message
      description: "Types: text, image, video, voice_note, venue_card. Media types
        need a `media_asset_id` from a completed upload that you own and that
        was uploaded as the matching kind. Voice notes carry `duration_ms`.
        Spends daily message quota, refunded if the write fails. Answers 403
        with an identical message for every closed-conversation reason —
        blocked, expired, unmatched — so a block cannot be confirmed by
        elimination."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  enum:
                    - text
                    - image
                    - video
                    - voice_note
                    - venue_card
                  default: text
                body:
                  type: string
                  minLength: 1
                  maxLength: 2000
                media_asset_id:
                  type: string
                  format: uuid
                venue_id:
                  type: string
                  format: uuid
                duration_ms:
                  type: integer
                  exclusiveMinimum: true
                  minimum: 0
                  maximum: 600000
                moderation_overridden:
                  type: boolean
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "422":
          description: QUOTA_EXCEEDED — You have reached your limit for today.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: QUOTA_EXCEEDED
                  message: You have reached your limit for today.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/conversations/{id}/read:
    post:
      tags:
        - Chat
      summary: Mark read
      description: Clears your unread badge and stamps `read_at` on the other person’s
        messages so their ticks update. Never stamps your own.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/moderation/check:
    post:
      tags:
        - Moderation
      summary: Check content before sending
      description: '**Advisory, never blocking.** `can_send` is always true, at every
        severity — show "Edit message" / "Send anyway" from `should_warn` and
        let the user decide. Send `overridden: true` when they push past a
        warning; that record is what the moderation team needs later. Content is
        hashed, never stored. If the provider is unreachable the call still
        succeeds with `timed_out: true` and severity `none`: a third-party
        outage must never cost a user their message. Scam and payment checks run
        on every mode, not just dating.'
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                content:
                  type: string
                  minLength: 1
                  maxLength: 4000
                subject_type:
                  type: string
                  enum:
                    - message
                    - bio
                    - prompt_answer
                    - display_name
                    - photo
                  default: message
                subject_id:
                  type: string
                  format: uuid
                overridden:
                  type: boolean
              required:
                - content
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/moderation/flags:
    get:
      tags:
        - Moderation
      summary: The moderation queue
      description: System-raised flags, distinct from user reports. Moderator or admin
        only — answers 403, not 404, because an admin surface is documented and
        there is nothing to conceal about its existence.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/moderation/flags/{id}:
    patch:
      tags:
        - Moderation
      summary: Resolve a flag
      description: Moves a flag to under_review, actioned, or dismissed, and records
        the moderator. Moderator or admin only.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - under_review
                    - actioned
                    - dismissed
              required:
                - status
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications:
    get:
      tags:
        - Notifications
      summary: The notification feed
      description: Every notification is persisted here AND pushed — never pushed
        alone. A push banner is gone once dismissed, so this feed is the only
        place a notification can still be found afterwards. Cursor-paginated,
        newest first. `?unread_only=true` filters.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/unread-count:
    get:
      tags:
        - Notifications
      summary: Unread notification count
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/badges:
    get:
      tags:
        - Notifications
      summary: Badge counts for all five tabs
      description: Discover, Requests, Matches, Plans, Notifications, plus a total.
        Poll this on app resume rather than calling five endpoints. `total`
        deliberately EXCLUDES `discover` — cards waiting is not something the
        user is behind on, and counting it would make the app badge permanently
        non-zero.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/read-all:
    post:
      tags:
        - Notifications
      summary: Mark everything read
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/{id}/read:
    post:
      tags:
        - Notifications
      summary: Mark one read
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/preferences:
    get:
      tags:
        - Notifications
      summary: Per-category delivery preferences
      description: All eight categories, with defaults filled in for any the user has
        never changed — so the settings screen renders from one call and never
        has to know what the defaults are.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/preferences/{category}:
    patch:
      tags:
        - Notifications
      summary: Change a category
      description: "**Safety notifications cannot be muted** for push or in-app: they
        carry emergency and moderation outcomes, and someone missing the result
        of a report they filed is the failure this product cannot have. Their
        email channel can still be switched off."
      parameters:
        - name: category
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                push_enabled:
                  type: boolean
                email_enabled:
                  type: boolean
                in_app_enabled:
                  type: boolean
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/tokens:
    post:
      tags:
        - Notifications
      summary: Register an FCM token
      description: The token belongs to a DEVICE, not a user — one person may have
        several, each with its own token. The device must already be signed in.
        Re-registering a token that was on another device moves it, because FCM
        issues one token per install.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                device_id:
                  type: string
                  minLength: 1
                  maxLength: 128
                fcm_token:
                  type: string
                  minLength: 1
                  maxLength: 4096
              required:
                - device_id
                - fcm_token
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/notifications/tokens/{device_id}:
    delete:
      tags:
        - Notifications
      summary: Stop pushing to a device
      description: Clears the token WITHOUT ending the session. Revoking a device is
        the security action and lives at DELETE /devices/{id}.
      parameters:
        - name: device_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/reports:
    post:
      tags:
        - Safety
      summary: Report a user
      description: "**Reports are anonymous.** The reported user never learns who
        filed one, through any endpoint, notification, or error message.
        `also_block: true` blocks in the SAME transaction — a report filed
        without its block would leave someone still exposed to the person they
        just reported. Evidence must be completed uploads of kind
        `report_evidence`; a verification document cannot be attached."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reported_id:
                  type: string
                  format: uuid
                reason:
                  type: string
                  enum:
                    - harassment
                    - fake_profile
                    - spam_scam
                    - safety_concern
                description:
                  type: string
                  maxLength: 1000
                context_type:
                  type: string
                  enum:
                    - profile
                    - message
                    - call
                    - plan
                context_id:
                  type: string
                  format: uuid
                also_block:
                  type: boolean
                evidence_asset_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  maxItems: 5
              required:
                - reported_id
                - reason
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    get:
      tags:
        - Safety
      summary: Reports you filed
      description: Only reports YOU submitted. There is deliberately no endpoint for
        reports about you — even a count would tell someone they are under
        review.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/reports/review:
    get:
      tags:
        - Safety
      summary: The moderation queue
      description: The ONLY place a reporter identity is returned — moderators need it
        to spot coordinated reporting and retaliation. Moderator or admin only.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/reports/{id}:
    patch:
      tags:
        - Safety
      summary: Resolve a report
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - under_review
                    - actioned
                    - dismissed
                resolution_note:
                  type: string
                  maxLength: 1000
              required:
                - status
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/blocks:
    get:
      tags:
        - Safety
      summary: People you blocked
      description: Only blocks YOU created. Listing who blocked you would tell you
        exactly that, which is what the 404-not-403 rule exists to prevent.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    post:
      tags:
        - Safety
      summary: Block someone
      description: "Idempotent. Also unmatches an active match: a block alone would
        leave it visible and frozen, which is right for a lapsed match and wrong
        for one deliberately cut."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                user_id:
                  type: string
                  format: uuid
              required:
                - user_id
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/blocks/{user_id}:
    delete:
      tags:
        - Safety
      summary: Unblock
      description: Does NOT restore the match it severed. Unblocking means "I will see
        them again", not "undo everything" — the pair can simply match again.
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/contacts:
    get:
      tags:
        - Safety
      summary: Trusted contacts
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    post:
      tags:
        - Safety
      summary: Add a trusted contact
      description: Needs a phone or an email — a contact with no way to reach them is
        decoration, and the moment it matters is the moment nobody checks.
        Maximum five.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                phone:
                  type: string
                  minLength: 5
                  maxLength: 32
                email:
                  type: string
                  format: email
                  maxLength: 320
                relationship:
                  type: string
                  maxLength: 64
              required:
                - name
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: BAD_REQUEST — We could not process that request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: BAD_REQUEST
                  message: We could not process that request.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/contacts/{id}:
    patch:
      tags:
        - Safety
      summary: Edit a trusted contact
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    delete:
      tags:
        - Safety
      summary: Remove a trusted contact
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/location:
    post:
      tags:
        - Safety
      summary: Start sharing live location
      description: "**High-risk data.** Sharing never starts implicitly, every session
        carries a hard expiry set at creation, and starting again ends the
        previous one. Ending a session DESTROYS the position trail — the session
        row survives as the record that sharing happened, the movements do not.
        Maximum 8 hours."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                plan_id:
                  type: string
                  format: uuid
                duration_minutes:
                  type: integer
                  minimum: 5
                  maximum: 480
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/location/active:
    get:
      tags:
        - Safety
      summary: Your active sharing session
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/location/{id}:
    delete:
      tags:
        - Safety
      summary: Stop sharing
      description: Deletes the position trail. Stopping that leaves a history is not
        stopping.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/location/{id}/ping:
    post:
      tags:
        - Safety
      summary: Record a position
      description: Refused on an expired session rather than extending it — the TTL is
        the consent boundary, and a client that keeps sending must not push it
        outward.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                latitude:
                  type: number
                  minimum: -90
                  maximum: 90
                longitude:
                  type: number
                  minimum: -180
                  maximum: 180
                accuracy_metres:
                  type: integer
                  minimum: 0
                  maximum: 10000
              required:
                - latitude
                - longitude
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/location/{id}/trail:
    get:
      tags:
        - Safety
      summary: Your own trail
      description: Readable only by the person sharing. Trusted contacts are told
        sharing started; they get no endpoint returning coordinates, because a
        shareable link to a live position is exactly what this design avoids.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/safety/emergency:
    post:
      tags:
        - Safety
      summary: Raise an emergency
      description: Records the event, attaches a position if given, and alerts trusted
        contacts. Succeeds even with no contacts configured and says so —
        someone pressing this is having the worst moment this app will be part
        of, and an error is not an acceptable outcome.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  enum:
                    - help_requested
                    - location_shared
                    - check_in_missed
                note:
                  type: string
                  maxLength: 500
                latitude:
                  type: number
                  minimum: -90
                  maximum: 90
                longitude:
                  type: number
                  minimum: -180
                  maximum: 180
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    get:
      tags:
        - Safety
      summary: Your emergency history
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/plans:
    get:
      tags:
        - Plans
      summary: Your plans
      description: "Three tabs via `?tab=upcoming|pending|history`, plus
        `?drafts=true`. Drafts are separate from pending on purpose: pending
        waits on the other person, a draft waits on you."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    post:
      tags:
        - Plans
      summary: Create a plan
      description: "Without `propose: true` this is a DRAFT, visible only to you and
        generating no notification. Requires a venue or a location, and a future
        time before it can be proposed."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                match_id:
                  type: string
                  format: uuid
                venue_id:
                  type: string
                  format: uuid
                custom_location:
                  type: string
                  minLength: 1
                  maxLength: 200
                custom_address:
                  type: string
                  maxLength: 300
                scheduled_at:
                  type: string
                  format: date-time
                duration_minutes:
                  type: integer
                  minimum: 15
                  maximum: 1440
                notes:
                  type: string
                  maxLength: 1000
                propose:
                  type: boolean
              required:
                - match_id
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/plans/{id}:
    get:
      tags:
        - Plans
      summary: One plan
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    patch:
      tags:
        - Plans
      summary: Edit a plan
      description: Creator only, and only while it is a draft or a proposal. Editing
        after acceptance would let one side change the time the other agreed to.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                venue_id:
                  type: string
                  format: uuid
                custom_location:
                  type: string
                  minLength: 1
                  maxLength: 200
                custom_address:
                  type: string
                  maxLength: 300
                scheduled_at:
                  type: string
                  format: date-time
                duration_minutes:
                  type: integer
                  minimum: 15
                  maximum: 1440
                notes:
                  type: string
                  maxLength: 1000
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/plans/{id}/propose:
    post:
      tags:
        - Plans
      summary: Send a draft
      description: The only way the other person learns the plan exists.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/plans/{id}/respond:
    post:
      tags:
        - Plans
      summary: Accept or decline
      description: Only the person who did NOT propose it may respond — otherwise
        someone could produce a confirmed meeting the other never agreed to.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                accept:
                  type: boolean
              required:
                - accept
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/plans/{id}/cancel:
    post:
      tags:
        - Plans
      summary: Cancel a plan
      description: Either participant, any time before completion.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  maxLength: 500
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/plans/{id}/share:
    post:
      tags:
        - Plans
      summary: Share with trusted contacts
      description: Confirmed plans only. Telling someone’s family about a plan that
        was never accepted is noise, and it leaks the other person’s
        availability before they agreed to anything.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                contact_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  minItems: 1
                  maxItems: 5
              required:
                - contact_ids
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/venues:
    get:
      tags:
        - Venues
      summary: Search venues
      description: Distance-sorted, nearest first, in METRES. Filter by `category` and
        `mode` — a venue tagged for study_buddy will not surface in cuddle. Pass
        `latitude`/`longitude` to browse another area without moving your
        profile location.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: BAD_REQUEST — We could not process that request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: BAD_REQUEST
                  message: We could not process that request.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/venues/saved:
    get:
      tags:
        - Venues
      summary: Your saved venues
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/venues/suggest/{match_id}:
    get:
      tags:
        - Venues
      summary: Suggestions for a match
      description: Tuned to the mode the match shares. Centred on YOUR location, not a
        midpoint — computing a midpoint needs the other person’s coordinates,
        and exposing where someone lives, even indirectly, is not worth the
        convenience.
      parameters:
        - name: match_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/venues/{id}:
    get:
      tags:
        - Venues
      summary: One venue
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/venues/{id}/save:
    post:
      tags:
        - Venues
      summary: Save for later
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    delete:
      tags:
        - Venues
      summary: Remove from saved
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/subscriptions/products:
    get:
      tags:
        - Subscriptions
      summary: What is on sale
      description: "The catalogue the paywall renders: Basic and Premium, monthly and
        yearly. Prices are integer MINOR UNITS plus a currency — never floats —
        and are INFORMATIONAL, because whoever takes the payment decides what is
        actually charged. Readable without a token, since the paywall is shown
        before some sign-in flows."
      security: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/subscriptions/me:
    get:
      tags:
        - Subscriptions
      summary: Your subscription
      description: "Read `is_active` rather than inferring from `status`: a cancelled
        subscription is still active until its period ends, and one in billing
        retry still entitles. Purchasing happens outside this API, so there is
        no endpoint here that starts one — this reports what the backend has
        been told."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/calls:
    post:
      tags:
        - Calls
      summary: Start a video call
      description: Creates the call and returns a room token for YOU. The body names a
        **match**, never a user and never a room — a client-named room is how
        someone gets a token for a call they were not invited to. The other
        person receives a `call:incoming` socket event and a push carrying the
        same `call_id`; treat both as one call. Calling an already-live call on
        the same match returns that call rather than starting a second one,
        because two rooms for one conversation means two people in different
        rooms.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                match_id:
                  type: string
                  format: uuid
              required:
                - match_id
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    get:
      tags:
        - Calls
      summary: Call history
      description: Newest first, cursor-paginated. Carries no token — a finished call
        has nothing to join. A call that rang out reads as `missed` even before
        the sweep rewrites the column, so history never shows something as still
        ringing. `duration_seconds` is null for a call that was never answered;
        zero would read as a call that connected silently.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/calls/{id}/answer:
    post:
      tags:
        - Calls
      summary: Answer
      description: Only the person who did NOT start the call may answer. Returns your
        room token. Answering a call that was declined, ended, or rang out is a
        409 rather than a silent no-op — the app needs to know why nothing
        happened.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/calls/{id}/decline:
    post:
      tags:
        - Calls
      summary: Decline
      description: Only the callee. Emits `call:declined`, which is deliberately
        distinct from `call:ended` so the caller can say "declined" rather than
        "call ended".
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/calls/{id}/end:
    post:
      tags:
        - Calls
      summary: Hang up
      description: "Either participant, at any live stage. **Idempotent** — both apps
        commonly send this on hang-up and the second must not surface an error.
        Duration is measured from when the call was ANSWERED, not from when it
        started ringing: a call that rang for forty seconds and was picked up
        for ten lasted ten."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/calls/{id}/token:
    get:
      tags:
        - Calls
      summary: Re-issue a room token
      description: For a reconnect, or a call outstaying its token. Tokens are
        short-lived and scoped to exactly ONE room; this endpoint is what makes
        that workable rather than merely strict. The permission check runs again
        on every issue, so blocking someone mid-call stops their next reconnect
        instead of taking effect after they hang up. Refused once the call is
        over — a token for a finished room is a credential with no purpose.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "409":
          description: CONFLICT — That already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: CONFLICT
                  message: That already exists.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/calls/{id}/safety:
    post:
      tags:
        - Calls
      summary: In-call safety action
      description: "`flag`, `end_and_report`, or `send_live_update` (spec §5.7). The
        action is recorded before anything else happens, because the record is
        the point — a pattern of flags against one account is what moderation
        acts on. `end_and_report` ends the call FIRST and then files the report:
        someone reaching for this wants the call to stop. `note` is optional on
        purpose; a person reaching for a safety control mid-call is not in a
        position to write an explanation. The reported user is never told who
        reported them."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                action:
                  type: string
                  enum:
                    - flag
                    - end_and_report
                    - send_live_update
                note:
                  type: string
                  maxLength: 500
              required:
                - action
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/me/entitlements:
    get:
      tags:
        - Entitlements
      summary: What this user’s plan includes
      description: "The whole plan in one call: every feature flag, every daily quota
        with what is left of it, and the tier. Render paywalls and
        remaining-swipe counters from this rather than inferring them from 403s
        and 422s. Numeric values of -1 mean unlimited. Quotas reset at UTC
        midnight, given exactly by `resets_at`."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/modes:
    get:
      tags:
        - Modes
      summary: All eight modes with their state
      description: Every mode, whether it is enabled, its per-mode preferences, and
        the label to render for its deck action. `can_enable` is false when a
        mode needs verification the user does not have — grey the toggle out
        rather than letting them tap it into a 403.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/modes/{mode}:
    get:
      tags:
        - Modes
      summary: One mode
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    patch:
      tags:
        - Modes
      summary: Enable, disable, or configure a mode
      description: "Preferences are PER MODE — a 5km radius in Cuddle and 50km in
        Networking is normal. `preferences` holds mode-specific extras validated
        against that mode: relationship_goal for dating, subject and
        academic_level for study_buddy, pet_type for pet_dates, instruments for
        trading. An unknown key is rejected, not ignored. Enabling beyond your
        plan returns PREMIUM_REQUIRED with upgrade context; Cuddle returns
        FORBIDDEN without verification."
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                is_enabled:
                  type: boolean
                min_age:
                  type: integer
                  minimum: 18
                  maximum: 120
                max_age:
                  type: integer
                  minimum: 18
                  maximum: 120
                radius_metres:
                  type: integer
                  exclusiveMinimum: true
                  minimum: 0
                  maximum: 500000
                verified_only:
                  type: boolean
                preferences:
                  type: object
                  additionalProperties: {}
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "403":
          description: FORBIDDEN — You do not have access to this.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: FORBIDDEN
                  message: You do not have access to this.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/modes/{mode}/primary:
    post:
      tags:
        - Modes
      summary: Set the primary mode
      description: Exactly one at a time. The mode must already be enabled.
      parameters:
        - name: mode
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/settings:
    get:
      tags:
        - Settings
      summary: All settings
      description: Appearance, privacy, discovery, language, and snooze state. Stored
        server-side so they follow the user to a new device instead of resetting
        on reinstall.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    patch:
      tags:
        - Settings
      summary: Update settings
      description: Send only what changed. distance_unit is display only — the API
        always returns metres.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                theme:
                  type: string
                  enum:
                    - system
                    - light
                    - dark
                text_scale:
                  type: number
                  minimum: 0.8
                  maximum: 2
                reduce_motion:
                  type: boolean
                high_contrast:
                  type: boolean
                distance_unit:
                  type: string
                  enum:
                    - miles
                    - kilometres
                show_distance:
                  type: boolean
                show_last_active:
                  type: boolean
                incognito:
                  type: boolean
                global_verified_only:
                  type: boolean
                pause_new_matches:
                  type: boolean
                language:
                  type: string
                  pattern: ^[a-z]{2}(-[A-Za-z0-9]{2,8})?$
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/settings/snooze:
    post:
      tags:
        - Settings
      summary: Hide from all decks
      description: The account stays active and existing matches and conversations
        survive. Omit ends_at to snooze until manually resumed.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ends_at:
                  type: string
                  format: date-time
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
    delete:
      tags:
        - Settings
      summary: Resume
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/devices:
    get:
      tags:
        - Settings
      summary: Connected devices
      description: Send X-Device-Id and the current device is flagged with is_current,
        so the app can label it and stop the user signing themselves out by
        accident.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/devices/others:
    delete:
      tags:
        - Settings
      summary: Sign out everywhere else
      description: Keeps the device making the request. Ends every other session for real.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
  /api/v1/devices/{id}:
    delete:
      tags:
        - Settings
      summary: Revoke one device
      description: Ends that session rather than only removing it from a list — the
        refresh token family bound to the device is revoked too.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessEnvelope"
        "400":
          description: VALIDATION_FAILED — Some fields need attention.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: VALIDATION_FAILED
                  message: Some fields need attention.
                  details: null
        "401":
          description: AUTH_TOKEN_INVALID — Your session is no longer valid. Please sign
            in again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: AUTH_TOKEN_INVALID
                  message: Your session is no longer valid. Please sign in again.
                  details: null
        "404":
          description: NOT_FOUND — We could not find what you were looking for.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: NOT_FOUND
                  message: We could not find what you were looking for.
                  details: null
        "429":
          description: RATE_LIMITED — Too many requests. Please try again shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: RATE_LIMITED
                  message: Too many requests. Please try again shortly.
                  details: null
        "500":
          description: INTERNAL_ERROR — Something went wrong on our end.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                success: false
                error:
                  code: INTERNAL_ERROR
                  message: Something went wrong on our end.
                  details: null
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: The access_token from /auth/login. Lives 30 minutes; refresh with
        /auth/refresh.
  schemas:
    SuccessEnvelope:
      type: object
      required:
        - success
        - data
        - meta
      properties:
        success:
          type: boolean
          enum:
            - true
        data:
          description: Always an object or array, never a bare scalar.
        meta:
          nullable: true
          type: object
          properties:
            pagination:
              type: object
              properties:
                next_cursor:
                  type: string
                  nullable: true
                has_more:
                  type: boolean
                limit:
                  type: integer
    ErrorEnvelope:
      type: object
      required:
        - success
        - error
      properties:
        success:
          type: boolean
          enum:
            - false
        error:
          type: object
          required:
            - code
            - message
            - details
          properties:
            code:
              type: string
              enum:
                - VALIDATION_FAILED
                - BAD_REQUEST
                - AUTH_REQUIRED
                - AUTH_TOKEN_EXPIRED
                - AUTH_TOKEN_INVALID
                - AUTH_INVALID_CREDENTIALS
                - FORBIDDEN
                - ONBOARDING_INCOMPLETE
                - ACCOUNT_SUSPENDED
                - PREMIUM_REQUIRED
                - USER_BLOCKED
                - NOT_FOUND
                - CONFLICT
                - FILE_TOO_LARGE
                - UNSUPPORTED_MEDIA_TYPE
                - QUOTA_EXCEEDED
                - RATE_LIMITED
                - INTERNAL_ERROR
                - SERVICE_UNAVAILABLE
              description: Branch on this. Stable contract — never branch on `message`.
            message:
              type: string
              description: Displayable to the user. May change.
            details:
              nullable: true
              type: object
              description: For VALIDATION_FAILED, keyed by field name with a list of messages.
                For QUOTA_EXCEEDED, paywall context.
    AuthTokens:
      type: object
      required:
        - access_token
        - refresh_token
        - token_type
        - expires_in
      properties:
        access_token:
          type: string
        refresh_token:
          type: string
          description: Rotates on every use. Always store the newest one.
        token_type:
          type: string
          enum:
            - Bearer
        expires_in:
          type: integer
          description: Seconds until the access token expires.
